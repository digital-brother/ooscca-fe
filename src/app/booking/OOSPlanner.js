import { Box, Container, Typography } from "@mui/material";

export default function OOSPlanner() {
  return (
    <Box>
      <Container sx={{ my: 10 }}>
        <Typography variant="overlineBold">Directly from calendar</Typography>
        <Typography variant="h2">Enjoy the ease of booking activities without the text and email tennis</Typography>
      </Container>
    </Box>
  );
}
